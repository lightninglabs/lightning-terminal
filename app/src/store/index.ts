// export the root store and createStore function
export { Store, createStore } from './store';
// re-export from provider
export { StoreProvider, useStore } from './provider';
// re-export all of the child stores
export * from './stores';
